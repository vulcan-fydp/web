overwrite: true
schema: "./node_modules/@vulcan-fydp/schema/signal_schema.gql"
documents: "src/**/*.relay.gql"
generates:
  src/graphql.ts:
    plugins:
      - "typescript"
  src/:
    preset: near-operation-file
    presetConfig:
      baseTypesPath: graphql.ts
    plugins:
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      defaultBaseOptions:
        context:
          target: relay
  ./graphql-relay.schema.json:
    plugins:
      - "introspection"
