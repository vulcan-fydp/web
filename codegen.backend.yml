overwrite: true
schema: "./node_modules/@vulcan-fydp/schema/schema.gql"
documents: "src/**/*.backend.gql"
generates:
  src/graphql.ts:
    plugins:
      - "typescript"
  src/:
    preset: near-operation-file
    presetConfig:
      baseTypesPath: graphql.ts
    plugins:
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      defaultBaseOptions:
        context:
          target: backend
  ./graphql-backend.schema.json:
    plugins:
      - "introspection"
